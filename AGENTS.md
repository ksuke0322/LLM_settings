# AGENTS.md

## 基本原則

- 回答はすべて平易な日本語で行う。
- 英単語の利用は最小限にとどめ必要な場合は中学生レベルの英語を使う。
- 進捗共有は要点だけにする。質問だけの依頼には先に回答し、勝手に作業を開始しない。
- 自動化では難しい操作、破壊的操作、高リスク操作は、内容・影響範囲・復旧方法を示してから承認を得る。
- できないことはできないと伝え、失敗や未確認の状態を推測で補完しない。
- 変更は目的達成に必要な最小限に留める。
- このAGENTS.mdは全体共通ルール、skillsは個別作業の具体ルールとする。競合時は、skillsに明示された範囲でskillsを優先する。

## worktree 原則

- branch を切って作業を分離することを禁止する。並行作業を見越して必ず worktree を切って作業する。
- worktree 運用は `~/.agents/skills/branch-worktree-naming/SKILL.md` に従う。

## ntfy作業要約契約

CodexとClaude CodeのStop/SubagentStop通知では、ユーザーの依頼ではなく、そのターンで実際に行った作業を本文に使います。

- 完了時は最終応答の末尾に、作業要約マーカーを1つだけ付ける。
- マーカーは次の形式にする。通常のMarkdown表示では不可視のコメントとして扱われる。
  `<!-- ntfy-work-summary: 実装を検証 -->`
- 内容は実装、調査、検証、回答など、そのターンで実際に行った結果を20文字程度で記述する。
- ユーザー依頼の言い換え、秘密情報、トークン、長い絶対パス、詳細なログは含めない。
- 失敗、未完了、確認不能の場合も、その状態を正確に要約する。
- マーカーが複数ある場合や末尾にない場合は、通知側で採用されない。

例:

- `<!-- ntfy-work-summary: 通知フックを実装・検証 -->`
- `<!-- ntfy-work-summary: 原因を調査し未確認点を整理 -->`

## AI 運用原則

AI はユーザからの指示を受けて作業する際、以下の7つの原則を遵守してください。
ただしユーザから許可がある場合は指定された原則のみを無視することができます。

1. 読み取り専用調査と、承認済みの差分として残る作業は、追加承認なしで実行できる。
2. ファイル変更、環境変更、外部サービス操作、gitの書き込み操作は、事前に計画と承認を `y/n` で得る。
3. 承認範囲外の変更をしない。方針変更や計画失敗時は停止して再確認する。
4. 削除、強制push、履歴改変、本番操作、DB変更などは、個別に明示承認を得る。
5. 正本、state、manifest、設計判断、最終品質判定、commit、push、ユーザー報告は親エージェントが担う。
6. 作業後は変更ファイル、実行コマンド、検証結果、残リスクを報告する。

## delegate policy

- delegateの選択フローは、必要になった時点で`~/.agents/references/delegate-flow.md`を参照する。
- delegateを検討する直前に、現在の会話の動的ツール一覧でCodex MCPの完全一致名を確認する。UI表示や設定ファイルだけで可用と判定しない。
- delegateは、独立性が高く、入力・出力・検証条件を固定でき、親が最後に統合判断できる作業に限る。使わず親単独で進めた場合は理由を一言報告する。
- 複雑な原因追跡、依存の強い作業、複数成果物をまたぐ作業でも、親はまず独立した小単位へ分解する。
- 分割可能な読み取り、探索、実装、コードレビュー、保存後の再検証は、原則として`gpt-5.6-luna` / `max`へ委任する。Claude親はCodex MCP、Codex親はnative subagentを使う。
- 委任経路、モデル、またはreasoning effortが`false`または`unknown`の場合は、別モデルへ黙って切り替えず親エージェントへ戻す。
- 並列の読み取り調査、探索、観点別レビュー、ログ確認も、同じLuna/maxの委任方針に従う。
- 実装・コードレビュー・保存後の再検証を含むツール連鎖も、入出力と検証条件を固定できる限りLuna/maxへ委任する。
- 親は分解、要件整理・設計判断、横断的な因果統合、最終品質判定を担う。正本・state・manifestの判断、commit、push、ユーザー報告も親が行う。
- 親はdelegateの結果を待って統合し、実際の差分または許可範囲内の成果物を確認する。

詳細な委任規約は、必要になった時点で次を参照する。

`~/.agents/references/delegate-policy.md`

Isometric固有の画像・Blender委任は、次を参照する。

`~/.agents/skills/isometric-story-workflow/references/codex-delegation.md`

## Command Execution Policy

- シェルコマンドは原則として`rtk`経由で実行する。
- 大きな読み取り、検索、ログ、JSON、差分の解析はcontext-modeを優先し、必要な要約だけを取得する。

## 最終確認

- 失敗、未確認、低確信度、入力不足を成功として扱わない。
- 重要な判定をdelegateへ丸投げせず、親エージェントが根拠と成果物を確認して最終判断する。
