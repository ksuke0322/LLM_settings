# Blender用プロンプトDB保存テンプレート(本番制作差分)

ステップ5でBlender用プロンプトDB配下に新規ページを作る際、このテンプレートを埋めてから保存する。**空欄・「TBD」・「後で決める」が残った状態での保存は不可**(ワークシート曖昧さチェックリストと同じ基準)。

このページは**設計書(worksheet)と二重管理しない**。オブジェクト表・寸法比例表・World/ライティング表・配色方針・質感/構造/アセット表・Story Beat Sheet・Acceptance Matrixなどの設計内容は**設計書ページを正本**とし、ここには再転記しない。このページに書くのは、設計書に載らない**本番制作固有の差分**(制作パス・state遷移の実フレーム・クール間引き継ぎ手順・manifest計画)と、設計書への参照だけ。ルール本体(「どう作るか」)は`blender-isometric-rules`スキルが自動で効くため一切書かない。

対応する設計書ページ: `<設計書ページへのリンク(必須)>`

---

## 1. 制作識別・パス(本番固有)

- テーマ名 / 対象クール一覧: `<例: 竹: 小さな灯台 / クール1〜3。1ページで全クール共通>`
- 作業ディレクトリ: `/Users/sawairikeisuke/Documents/Blender/isometric/test_gen_ai/<英文表記>/`
- `.blend`ファイル名: `<例: lighthouse.blend>`
- ビジュアルスタイル(設計書で確定済みの値を再掲): `<トイクレイ調 / フラットトゥーン>`
- 世界観リファレンス画像パス: `<test_gen_ai/<名>/ref/world_reference.png>`
- クール別チェック画像 予定パス(全クール): `<例: cool1=…/ref/cool1_reference.png。本体は各クールのステップ6で生成>`

## 2. 設計内容は設計書を参照(再転記しない)

ステップ6のfetchでは**設計書ページとこのページの両方**を取得する。以下は設計書ページの該当節を正本として参照する(このページに表を複製しない):

| 内容 | 正本の所在(設計書ページ内) |
|---|---|
| 寸法比例表(基準値・全比率・`STAGE_EXTENT`) | 設計書「寸法比例表」 |
| オブジェクト一覧(署名ディテール・作り込みティア含む) | 設計書「オブジェクト一覧表」 |
| 背景ディテール構成(種類・配置平面・除外ゾーン) | 設計書「背景ディテール」 |
| Collection構成(`common_environment`含む) | 設計書「Collection構成表」 |
| World・ライティング | 設計書「World・ライティング設計表」 |
| 配色・画面方針 | 設計書「配色・画面方針メモ」 |
| 質感タイプ・構造タイプ・アセット方針(PolyHaven ID) | 設計書「質感タイプ・構造タイプ・アセット方針表」 |
| Story Beat Sheet・時間設計 | 設計書「Story Beat Sheet」 |
| Reference Pack・Acceptance Matrix | 設計書の該当節 / `quality-gates.md` |

- 制作で基準値の実数が必要な場合のみ、基準値変数1つの算出実数をここに一行記す(残りは比率から機械的に算出): `<例: TOWER_HEIGHT = 3.6>`

## 3. state構成(遷移内容と実フレーム方針・本番固有)

1. state_1: `<何もない状態 → 何が完成するか / トランジション種別>`
2. state_2: `<...>`
...

- 1フレーム目から最初の遷移を開始し、初期stateを静止表示しないこと。
- 各stateの遷移区間(目安1.0〜1.5秒、強化対象は2.0〜2.5秒)と開始/終了frameを明記する。

## 4. クール間引き継ぎ(クール2以降のみ・本番固有)

- 引き継ぐオブジェクト一覧: `<...>`
- 静的スケール(地形・水面等、`common_environment`所属)でリセット対象から除外するもの: `<...>`
- 前クール共有マテリアルの要否(全要素に明記): `<...>`
- 引き継いだ背景ディテールスキャッターに追加する除外ゾーン(座標範囲): `<設計書「背景ディテール」の除外ゾーンと整合>`

## 5. 再現manifest・品質ゲート証跡計画(本番固有)

- Manifest予定パス(全クール): `<作業ディレクトリ直下/cool<N>_manifest.json>`
- Schema: `references/manifest-schema.md`のversion 1
- Blender version / render engine / Color Management / seed: `<値>`
- PolyHaven asset ID / resolution / 取得日: `<一覧。未使用なら空配列>`
- 各品質ゲート証跡の予定絶対パス(未実施gateはmanifestで`pending`): `<story_beat / animatic / spike / visual / common_sense / still / motion / story_final / app>`

---

## 保存前の最終確認(抜け漏れ防止)

- [ ] 対応する設計書ページへのリンクが設定されている
- [ ] 1〜5節に空欄・TBDがない
- [ ] 設計内容(寸法・オブジェクト・World等)をこのページに再転記せず、設計書参照に留めている
- [ ] 3節のstate遷移フレームが全クール分、設計書のStory Beat Sheetと矛盾しない
- [ ] クール2以降の場合、4節(引き継ぎ)が埋まっている
- [ ] 5節に全クールmanifestと各ゲート証跡の予定絶対パスが記録され、未実施gateは`pending`扱いと明記されている
- [ ] このページ内にルール本体(「どう作るか」の手順文)を書いていない(`blender-isometric-rules`スキルとの二重管理を避ける)
